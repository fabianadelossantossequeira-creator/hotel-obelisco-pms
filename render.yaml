services:
  - type: web
    name: hotel-obelisco-api
    runtime: python
    region: oregon
    buildCommand: apt-get update && apt-get install -y gcc && pip install -r backend/requirements.txt
    startCommand: cd backend && uvicorn app.main:app --host 0.0.0.0 --port $PORT
    plan: free
